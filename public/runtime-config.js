// For a separately hosted static UI, set this to the public Audora API URL.
// Example: window.AUDORA_RUNTIME_CONFIG = { apiBaseUrl: "https://api.example.com" };
window.AUDORA_RUNTIME_CONFIG = window.AUDORA_RUNTIME_CONFIG || { apiBaseUrl: "" };
