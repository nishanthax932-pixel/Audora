{
  "name": "audora",
  "version": "0.1.0",
  "private": true,
  "description": "Audora — an online, configurable AI assistant.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
